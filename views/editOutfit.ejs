<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Outfit</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">StyleIt!</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/outfits">Outfits!</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        <h2>Edit Outfit</h2>
        <form action="/editOutfit/<%= outfit.outfitID %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="outfitName">Outfit Name</label>
                <input type="text" class="form-control" id="outfitName" name="outfitName" placeholder="Enter outfit name" value="<%= outfit.outfitName %>" required>
            </div>
            <div class="form-group">
                <label for="outfitDescription">Outfit Description</label>
                <textarea class="form-control" id="outfitDescription" name="outfitDescription" rows="3" placeholder="Enter outfit description"><%= outfit.outfitDescription %></textarea>
            </div>
            <div class="form-group">
                <label for="outfitImage">Current Outfit Image</label>
                <br>
                <% if (outfit.outfitImage) { %>
                    <img src="/images/<%= outfit.outfitImage %>" alt="Current Outfit Image" style="max-width: 300px; max-height: 300px;">
                <% } else { %>
                    <p>No image uploaded for this outfit.</p>
                <% } %>
            </div>
            <div class="mb-3">
                <label for="newOutfitImage" class="form-label">Upload New Outfit Image</label>
                <input type="file" class="form-control" id="outfitImage" name="outfitImage" accept="image/*" value="<%= outfit.outfitImage %>">
            </div>
            <button type="submit" class="btn btn-primary">Save Changes</button>
        </form>
    </div>
</body>
</html>
